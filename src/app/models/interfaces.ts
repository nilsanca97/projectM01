export interface User {
    name: string;
    email: string;
    password: string;
}
export interface Chuleton {
    tipo: string;
    origen: string;
    peso: number;
    maduracion: number;
    cantidad: number;
}